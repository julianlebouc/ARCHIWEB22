import { SearchBarDirectiveDirective } from './search-bar-directive.directive';

describe('SearchBarDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchBarDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
